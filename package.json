{
  "name": "hair-ecosystem",
  "version": "1.0.0",
  "private": true,
  "description": "HairEco - Beauty & Salon Ecosystem connecting salon owners, professionals, shops, and customers",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:web\"",
    "dev:server": "cd server && npm run dev",
    "dev:web": "cd web && npm run dev",
    "build": "npm run build:shared && npm run build:server && npm run build:web",
    "build:shared": "cd shared && tsc",
    "build:server": "cd server && npm run build",
    "build:web": "cd web && npm run build",
    "db:push": "cd server && npx prisma db push",
    "db:seed": "cd server && npx ts-node prisma/seed.ts"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  }
}
